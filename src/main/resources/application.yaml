server:
  servlet:
    context-path: /api
spring:
  application:
    name: ondedoar

  logging:
    level:
      org:
        springframework:
          security: DEBUG
  cache:
    type: redis

  app:
    frontend:
      url: https://www.ondedoar.org

  mail:
    host: smtp.gmail.com
    port: 587
    username: contatoondedoar@gmail.com
    password: jgvc sagj fixb yqyp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    repair-on-migrate: true
    encoding: UTF-8
    ignore-unsupported-database: true
    lock-retry-count: 10

  datasource:
    url: jdbc:postgresql://ondedoarprddb-01.cyhau6keq393.us-east-1.rds.amazonaws.com:5432/ondedoar
    username: root
    password: ondedoar14
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

  api-docs:
    enabled: true

jwt:
  public:
    key: classpath:keys/my.key.pub

  private:
    key: classpath:keys/my.key.priv